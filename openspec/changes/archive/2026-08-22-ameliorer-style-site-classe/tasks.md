## 1. Cadrage visuel et fondations CSS

- [x] 1.1 Definir les jetons de design (couleurs, typographie, espacements, rayons, ombres) dans `src/assets/site.css` et verifier que le site compile/sert sans erreur.
- [x] 1.2 Mettre en place l'echelle typographique globale (titres, texte courant, interlignage) et verifier visuellement sur `src/index.html` et `src/home/index.html` que la hierarchie est nette.

## 2. Navigation et interactions

- [x] 2.1 Restyler la navigation principale (desktop + mobile) avec etats hover/actif/focus explicites et verifier sur une page interne que chaque etat est perceptible.
- [x] 2.2 Ajuster, si necessaire, `src/assets/navigation.js` pour conserver l'ergonomie mobile et verifier qu'aucun debordement horizontal n'apparait sous 768px.

## 3. Composants de contenu et cohesion globale

- [x] 3.1 Harmoniser les blocs de contenu prioritaires (cartes, sections d'actualites, CTA) et verifier la coherence visuelle entre `src/home/index.html`, `src/actualites/index.html` et `src/fse/index.html`.
- [x] 3.2 Appliquer les ajustements de lisibilite mobile (taille de texte >= 16px, zones tactiles suffisantes) et verifier le comportement sur un viewport 375px.

## 4. Verification transversale et finition

- [x] 4.1 Effectuer une passe de controle sur un echantillon de pages representatif et verifier l'absence de regressions de contraste et de mise en page.
- [x] 4.2 Remplacer toutes les occurrences de `https://example.org` par `https://www.fse-cooperativescolaire-amailloux.com` dans les pages et metadonnees publiques et verifier le diff des fichiers modifies.
- [x] 4.3 Lancer une recherche globale sur `src/` et les artefacts SEO pour verifier qu'aucune occurrence `https://example.org` ne subsiste.
- [x] 4.4 Documenter les choix finaux de style et de domaine canonique dans la change note et verifier la couverture des exigences `specs/site-visual-identity/spec.md` et `specs/seo-information-architecture/spec.md`.



